<!-- mex-generated -->

This project's living wiki is `.mex/`. The real anchor is imported below — read it first.

@../.mex/AGENTS.md

<!-- The import is resolved relative to this file, i.e. `<root>/.omp/`.
     If `.mex/AGENTS.md` is missing, omp leaves the literal `@../.mex/AGENTS.md`
     token in place instead of erroring — the bridge fails silently.
     `mex check` verifies this file and its import target. -->
