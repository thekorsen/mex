<!-- mex-generated -->

- Read `.mex/ROUTER.md` before starting work — project state, routing table, behavioural contract.
- Prefer `mex graph scope "<task>"` over grepping or reading files to find code.
- Treat any source the graph returns as ALREADY READ; do not re-open those files.
- After meaningful work, run the GROW step from `.mex/ROUTER.md`.
- Never edit `.mex/` prose merely to make `mex check` pass — fix the drift it reports.
